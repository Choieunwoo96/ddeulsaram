# 이번 업데이트 적용 방법 (SEO 최적화 + 애드센스 준비)

## 이번에 바뀐 것

1. **사이트맵/robots.txt 추가** — 검색엔진(구글 등)이 사이트 구조를 파악하고
   방 상세 페이지까지 전부 찾아갈 수 있도록 `/sitemap.xml`, `/robots.txt`를
   자동으로 만들어줍니다. 방이 새로 생기면 사이트맵도 자동으로 최신 상태로
   갱신됩니다 (1시간마다).
2. **페이지별 SEO 메타데이터** — 지금까지는 모든 페이지 제목이 "뜰사람"으로
   똑같았는데, 이제 방 상세 페이지는 그 방 제목으로, 카테고리로 필터링하면
   "리그오브레전드 상대 구하기"처럼, 각 페이지에 맞는 제목/설명이 붙어서
   검색 결과에 더 잘 노출됩니다.
3. **개인정보처리방침 / 이용약관 페이지 추가** — `/privacy`, `/terms` 페이지를
   새로 만들고 푸터에 링크를 달았습니다. 구글 애드센스 심사에는 개인정보처리방침이
   사실상 필수예요.
4. **ads.txt 자리 마련** — `public/ads.txt`에 안내 주석만 있는 빈 파일을
   만들어뒀습니다. 애드센스 승인을 받으면 발급되는 값으로 채워 넣으면 됩니다
   (아래 "애드센스 심사 전 준비 상태" 참고).

SQL 실행은 필요 없어요. 다만 이번엔 **환경변수 한 개를 새로 추가**하는 게
좋습니다 (필수는 아니지만 강력 추천).

## 적용 순서

1. **(권장) Vercel에 환경변수 추가**
   - Settings → Environment Variables
   - Name: `NEXT_PUBLIC_SITE_URL`, Value: 지금 쓰고 계신 사이트 주소
     (예: `https://ddeulsaram96.vercel.app`, 끝에 슬래시 `/` 없이)
   - Production / Preview / Development 다 체크 → 저장
   - 이 값을 안 넣어도 코드에 기본값이 있어서 당장 작동은 하지만, 나중에
     실제 도메인(예: ddeulsaram.com)을 연결하면 **반드시 이 값도 그 도메인으로
     바꿔주셔야** 사이트맵이 정확해집니다.
2. 압축 해제한 새 `ddeulsaram` 폴더 안의 내용물을 전부 복사합니다.
3. 기존 `ddeulsaram` 폴더에 붙여넣고 **모두 덮어쓰기**. (`app/privacy`,
   `app/terms`, `public` 처럼 새로 생긴 폴더가 있으니 "폴더 구조 그대로"
   풀어주세요.)
4. 터미널에서:

   ```
   git add .
   git commit -m "SEO 최적화 (사이트맵/robots) + 개인정보처리방침/이용약관 추가"
   git push
   ```

5. 배포 완료 후 `사이트주소/sitemap.xml`, `사이트주소/robots.txt`,
   `사이트주소/privacy`, `사이트주소/terms`가 잘 열리는지 확인해보세요.

## 구글 애드센스 심사 전 준비 상태 검토

지금 사이트 자체는 애드센스가 요구하는 **기술적인 요건**은 대부분 갖췄어요:
HTTPS 적용됨, 모바일 반응형, 페이지마다 서버에서 미리 렌더링되어 검색엔진이
읽기 좋음, 이번에 사이트맵/robots.txt와 개인정보처리방침/이용약관도
추가했습니다.

다만 심사 통과 여부는 기술보다 **콘텐츠와 운영 상태**에 더 좌우돼요. 아래는
솔직한 체크리스트예요.

**갖춰진 것**
- 개인정보처리방침, 이용약관 (신규)
- 사이트맵, robots.txt (신규)
- 명확한 사이트 목적, 깔끔한 네비게이션
- 도박성 콘텐츠 금지 등 정책 위반 소지 최소화
- 모바일 대응, HTTPS

**아직 부족해서 신경 써야 하는 것**
- **실제 콘텐츠 양** — 지금은 방이 거의 없는 상태예요. 애드센스는 "충분한
  양의 독창적인 콘텐츠"를 요구하는데, 빈 사이트에 가까우면 반려될 가능성이
  높습니다. 최소 며칠~몇 주 동안 방/매칭 글이 실제로 쌓이고 나서 신청하는
  걸 권장해요.
- **실제 방문자/트래픭** — 애드센스는 사람이 실제로 방문해서 읽을 콘텐츠인지도
  봅니다. 지인들에게 공유하거나 커뮤니티에 홍보해서 어느 정도 사용 흔적을
  만들어두면 유리해요.
- **사이트 운영 기간** — 만든 지 너무 얼마 안 된 사이트는 반려되는 경우가
  흔합니다. 보통 몇 주 정도는 운영해보고 신청하는 걸 추천해요.
- **소개(About) 페이지** — "이 서비스가 무엇인지" 설명하는 페이지가 있으면
  더 좋아요. 원하시면 다음에 만들어드릴 수 있어요.
- **ads.txt 실제 값** — 애드센스에 가입하고 승인 절차를 시작하면
  "사이트 추가" 메뉴에서 `pub-` 로 시작하는 퍼블리셔 ID를 받게 되는데, 그
  값으로 `public/ads.txt`를 채워야 최종 승인/광고 표시가 됩니다. (지금은
  안내 주석만 있는 빈 파일 상태예요.)

**제안하는 순서**
1. 지금처럼 사이트를 계속 다듬고, 실제로 몇 개의 방/매칭 글을 올려서
   콘텐츠를 쌓아보세요 (테스트 말고 실제 사용해볼 만한 내용으로).
2. 지인이나 커뮤니티에 공유해서 방문자를 좀 모아보세요.
3. 1~2주 정도 운영해본 뒤 https://www.google.com/adsense 에서 사이트를
   등록하고 심사를 신청하세요.
4. 승인되면 발급받은 `pub-` ID를 알려주시면, `public/ads.txt`와 광고 스크립트
   연동까지 제가 마무리해드릴게요.
