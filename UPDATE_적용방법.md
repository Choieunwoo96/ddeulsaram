# 이번 업데이트 적용 방법

이번에도 파일이 여러 개 바뀌어서, 폴더 전체를 새 걸로 통째로 바꾸는 방식으로 드립니다.
아래 순서대로만 하면 됩니다.

## 이번에 바뀐 것

1. **방장인데 수락/거절이 안 되던 버그 수정** — 방을 만들면 "방장 확인용 쿠키"를
   브라우저에 심어두는 방식이었는데, 그 쿠키를 심는 방식 자체에 빈틈이 있어서
   방장 본인도 가끔 인식이 안 되는 문제가 있었습니다. 쿠키를 훨씬 더 확실하게
   심어주는 방식으로 다시 만들었습니다 (방을 만들면 잠깐 `/api/rooms/.../claim`이라는
   주소를 한 번 거쳐서 방으로 이동하는데, 정상 동작입니다 — 눈에 띄는 변화는 없어요).
2. **방 삭제 기능** — 홈 화면 방 카드 오른쪽 위에 점 3개(⋮) 메뉴가 생겼습니다.
   내가 만든 방에서만 보이고, 눌러서 "삭제"를 선택하면 방이 삭제됩니다 (신청
   내역도 같이 삭제됩니다). 방 상세 페이지에도 방장에게만 "방 삭제" 버튼이 보입니다.
3. **자동매칭 등록 취소 기능** — `/match` 페이지의 대기열 목록에서도 내가 등록한
   항목에만 점 3개(⋮) 메뉴가 생겨서, 등록을 취소(삭제)할 수 있습니다.
4. **로고 변경** — 헤더/배너의 로고를 화살표 모양에서, 요청하신 대로 글러브 두 개가
   맞닿아 있는(fist bump) 모양으로 바꿨습니다.

⚠️ 2번(방 삭제)과 3번(대기열 취소) 기능 때문에 **Supabase SQL을 한 번 더
실행해야 합니다.** 실행 안 해도 사이트가 깨지진 않지만, 대기열 삭제 기능만
작동하지 않습니다.

## 적용 순서

1. **Supabase SQL 마이그레이션 실행** (지난번 `migration_host_token.sql`은 이미
   실행하셨으니 이번엔 새 파일만 실행하면 됩니다)
   - Supabase 프로젝트 → 왼쪽 메뉴 **SQL Editor** → **New query**
   - 새 zip 안의 `supabase/migration_2_entry_token.sql` 파일 내용 전체를 복사해서
     붙여넣고 **Run**
   - "Success" 메시지가 뜨면 완료.
2. 압축 해제한 새 `ddeulsaram` 폴더 안의 내용물을 전부 복사합니다.
3. 여러분 컴퓨터의 기존 `ddeulsaram` 폴더를 여세요.
   - **`.env.local`** 파일은 새 zip에 없어서 덮어써지지 않으니 그대로 두시면 됩니다.
4. 새로 받은 파일/폴더들을 기존 `ddeulsaram` 폴더 안에 붙여넣고, **모두 덮어쓰기**를
   선택하세요. (`app/api` 처럼 새로 생긴 폴더도 있으니, 압축 프로그램에서 "폴더
   구조 그대로 압축 풀기"로 붙여넣어 주세요.)
5. 터미널(cmd)을 열어 `ddeulsaram` 폴더로 이동한 뒤:

   ```
   git add .
   git commit -m "방장 인식 버그 수정 + 방/대기열 삭제 기능 + 로고 변경"
   git push
   ```

6. `git push`가 끝나면 Vercel이 자동으로 새로 빌드해서 배포합니다 (1~2분 소요).
   Vercel 프로젝트 화면에서 "준비됨"으로 바뀌면 사이트에 반영된 겁니다.
7. 배포된 사이트에서 **새 방을 하나 만들어서** 수락/거절 버튼이 보이는지, 방 카드에
   점 3개 메뉴가 뜨고 삭제가 되는지 확인해보세요. 기존에 만들어둔(이번 업데이트
   이전) 방들은 방장 구분이 안 되어 있을 수 있으니, 테스트는 새로 만든 방으로
   해주세요.

## 참고

- 방/대기열의 삭제 메뉴는 **그것을 만든 바로 그 브라우저**에서만 보입니다. 브라우저
  쿠키를 지우거나 다른 기기/브라우저로 접속하면 그 방(대기열)을 더 이상 관리할 수
  없게 됩니다 — 로그인 기능이 없는 프로토타입이라 생기는 한계입니다. 나중에
  로그인 기능을 붙이면 이 제약이 없어집니다.
