# 이번 업데이트 적용 방법

이번 업데이트는 파일 여러 개가 한꺼번에 바뀌어서, 예전처럼 한 줄씩 손으로
고치는 것보다 **폴더 전체를 새 걸로 통째로 바꾸는 게** 훨씬 안전하고 빠릅니다.
아래 순서대로만 하면 됩니다.

## 이번에 바뀐 것

1. **방 자동완료** — 방장이 수락(accepted)한 인원이 모집 인원(capacity)에 딱 차면
   방이 자동으로 "완료" 상태가 되고, 홈 화면 방 목록에서 사라집니다.
2. **자동매칭 안정화** — 매칭 등록 폼의 소분류가 자유입력에서 드롭다운으로 바뀌었고
   (방 만들기 폼과 동일한 방식), 저장되는 닉네임/지역/시간대 값이 항상 공백 없이
   저장되도록 고쳤습니다. 이제 조건이 같으면 확실히 매칭됩니다.
3. **로고 + 카테고리 아이콘** — 헤더와 홈 화면 배너에 "뜰사람" 로고(SVG)가 생겼고,
   카테고리마다 이모지 아이콘(🎮 🧩 🏃 🕹️ ⚔️)이 붙었습니다. 별도 이미지 파일을
   업로드할 필요 없이 코드 안에 전부 포함되어 있습니다.
4. **수락/거절은 방장만** — 지금까지는 신청 목록의 수락/거절 버튼이 아무나 누를 수
   있었는데, 이제 방을 만든 사람(방장)의 브라우저에서만 그 버튼이 보이고 실제로
   작동합니다. 로그인 기능이 없는 프로토타입이라 "방을 만든 그 브라우저"를
   쿠키로 기억해두는 방식입니다 (다른 사람 컴퓨터/브라우저에서는 그 방의 방장이
   될 수 없어요). 다른 사람들에게는 신청 상태만 "대기중"으로 보입니다.

   ⚠️ 이 기능 때문에 **Supabase에서 SQL을 한 번 더 실행해야 합니다** (아래 순서의
   1번). 실행 안 하면 새로 배포된 사이트에서 에러가 납니다.

## 적용 순서

1. **(중요, 먼저 하세요) Supabase SQL 마이그레이션 실행**
   - https://supabase.com 로그인 → 여러분의 `ddeulsaram` 프로젝트 열기
   - 왼쪽 메뉴 **SQL Editor** → **New query**
   - 새로 받은 zip 안의 `supabase/migration_host_token.sql` 파일을 메모장으로 열어
     내용 전체를 복사 → SQL Editor에 붙여넣고 **Run**
   - "Success" 메시지가 뜨면 완료. (기존 방 데이터는 그대로 유지되고, 컬럼 하나만
     추가되는 것이라 안전합니다. 다만 이 마이그레이션 이전에 만든 방들은 방장을
     구분할 방법이 없어서 그 방들에 한해서는 수락/거절 버튼이 더 이상 안 보입니다 —
     테스트용 방이라면 신경 안 쓰셔도 됩니다.)
2. 압축 해제한 새 `ddeulsaram` 폴더 안의 내용물을 전부 복사합니다.
3. 여러분 컴퓨터의 기존 `ddeulsaram` 폴더를 여세요.
   - **`.env.local`** 파일이 그 폴더 안에 있다면, 새 파일로 덮어쓰기 전에
     먼저 백업해두세요 (또는 그냥 안 지우고 두면 됩니다 — 새 zip에는
     `.env.local`이 들어있지 않아서 덮어써지지 않습니다).
4. 새로 받은 파일/폴더들을 기존 `ddeulsaram` 폴더 안에 붙여넣고, **모두 덮어쓰기**를
   선택하세요.
5. 터미널(cmd)을 열어 `ddeulsaram` 폴더로 이동한 뒤:

   ```
   git add .
   git commit -m "방 자동완료 + 자동매칭 개선 + 로고/아이콘 + 방장만 수락/거절"
   git push
   ```

6. `git push`가 끝나면 Vercel이 자동으로 새로 빌드해서 배포합니다 (1~2분 소요).
   Vercel 프로젝트 화면에서 "준비됨"으로 바뀌면 사이트에 반영된 겁니다.
7. 배포된 사이트에서 **새 방을 하나 만들어서** 수락/거절 버튼이 그 방을 만든
   브라우저(지금 쓰시는 브라우저)에서만 보이는지 확인해보세요. 시크릿창이나
   다른 브라우저로 같은 방에 들어가면 버튼 없이 "대기중"만 보이는 게 정상입니다.

## 앞으로 수정은 어떻게 하나요?

- 아주 작은 수정(문구 하나 바꾸기 등)은 다음에도 이렇게 채팅으로 요청하시면
  제가 안내해드릴 수 있습니다. 다만 이번처럼 **여러 파일이 같이 바뀌는 수정은
  앞으로도 이런 식으로 "새 버전 폴더 통째로 교체 + git push"** 방식으로 드리는 게
  훨씬 안전합니다 (지난번에 파일 한 줄만 손으로 고치다가 오타로 빌드가 깨진 적이
  있었죠 — 그런 실수를 줄이기 위함입니다).
- 요청하신 기능을 다 반영한 뒤에는 항상 이렇게: (1) 무엇이 바뀌었는지 요약,
  (2) 새 zip 파일, (3) 적용 방법을 같이 드릴게요.
- 코드를 직접 열어서 사소한 문구만 바꾸고 싶으시면, `app/` `components/` `lib/`
  폴더 안의 `.tsx`/`.ts` 파일에서 한글 문구 부분만 찾아 바꾸시면 됩니다 — 다만
  구조가 헷갈리시면 언제든 채팅으로 "이 문구를 이렇게 바꿔줘"라고 요청해주세요.
