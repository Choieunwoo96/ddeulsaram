# 이번 업데이트 적용 방법

이번에 추가된 건 **관리자 페이지(/admin)** 하나예요. 방장/등록자 구분 없이
모든 방과 매칭 대기열을 한 번에 정리할 수 있는 비밀번호로 보호된 페이지입니다.

## 이번에 바뀐 것

- **관리자 페이지 추가** — 사이트 주소 뒤에 `/admin`을 붙여서 들어가면
  (예: `https://ddeulsaram96.vercel.app/admin`) 비밀번호 입력창이 나오고,
  맞는 비밀번호를 입력하면 지금 있는 모든 방과 매칭 대기열 목록이 보이고
  각각 옆에 "삭제" 버튼이 있어요. 방장인지 아닌지, 어느 브라우저인지와
  상관없이 이 비밀번호만 알면 어디서든 정리할 수 있습니다.
- 헤더 메뉴에는 일부러 안 넣었어요 (아무나 눈에 띄지 않게). 주소를 직접
  입력해서 들어가시면 됩니다.

⚠️ 이 기능은 **비밀번호를 직접 정해서 환경변수로 등록**해야 작동합니다
(아래 순서의 1~2번). 이건 SQL이 아니라 Vercel 환경변수 설정이에요.

## 적용 순서

1. **관리자 비밀번호 정하기** — 원하는 비밀번호를 하나 정하세요 (남이 쉽게
   추측 못 할 걸로, 예: `ddeul2026!secure`). 이 비밀번호를 아는 사람은
   모든 방/대기열을 지울 수 있으니 잘 기억해두시고 남에게 알려주지 마세요.
2. **Vercel에 환경변수 추가**
   - Vercel 프로젝트 화면 → **Settings** → **Environment Variables**
   - Name: `ADMIN_PASSWORD`, Value: 방금 정한 비밀번호
   - Production / Preview / Development 세 개 다 체크 → **Save**
3. 압축 해제한 새 `ddeulsaram` 폴더 안의 내용물을 전부 복사합니다.
4. 여러분 컴퓨터의 기존 `ddeulsaram` 폴더를 여세요.
   - **`.env.local`** 파일은 새 zip에 없어서 덮어써지지 않으니 그대로 두시면 됩니다.
     다만 내 컴퓨터(`npm run dev`)에서도 관리자 페이지를 테스트하고 싶으시면,
     `.env.local` 파일을 열어서 `ADMIN_PASSWORD=방금정한비밀번호` 한 줄을
     직접 추가해주세요.
5. 새로 받은 파일/폴더들을 기존 `ddeulsaram` 폴더 안에 붙여넣고, **모두 덮어쓰기**를
   선택하세요. (`app/admin` 처럼 새로 생긴 폴더가 있으니, 압축 프로그램에서
   "폴더 구조 그대로 압축 풀기"로 붙여넣어 주세요.)
6. 터미널(cmd)을 열어 `ddeulsaram` 폴더로 이동한 뒤:

   ```
   git add .
   git commit -m "관리자 페이지 추가"
   git push
   ```

7. `git push`가 끝나면 Vercel이 자동으로 새로 빌드해서 배포합니다 (1~2분 소요).
8. 배포가 끝나면 `사이트주소/admin` 으로 들어가서, 2번에서 등록한 비밀번호로
   로그인해보세요. 로그인되면 지금 있는 방 1개, 대기열 3건이 보일 거예요 —
   삭제 버튼 눌러서 정리하시면 됩니다.

## 참고

- 로그인은 브라우저에 8시간 동안 유지됩니다. 다시 로그인하려면 비밀번호를
  또 입력하면 되고, 로그아웃 버튼도 있어요.
- 만약 환경변수(`ADMIN_PASSWORD`) 등록을 깜빡하고 로그인을 시도하면 "환경변수가
  설정되어 있지 않다"는 에러가 뜹니다 — 2번 순서를 다시 확인해주세요.
